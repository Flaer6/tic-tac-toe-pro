query getMyStats($mode: StatsMode) {
	getMyStats(mode: $mode) {
		losses
		totalGames
		winRate
		winStreak
		wins
		mode
	}
}

query GetUserStats($id: String!) {
	getUserStats(id: $id) {
		losses
		totalGames
		winRate
		winStreak
		wins
		mode
	}
}
